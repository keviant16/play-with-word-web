import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'play-with-words-web',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
